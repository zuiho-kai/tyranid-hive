# 错题本 — 流式处理 / 实时运行时

> 适用场景：改流式 LLM 输出处理、WebSocket 实时通道、VAD/ASR 管道

### 记录规则

- **条目结构**：❌ 错误做法 + ✅ 正确做法，整条控制在 **5 行以内**
- **❌/✅ 写法**：只写核心规则，禁止多层加粗嵌套（加粗最多一层）
- **`>` 注释**：只放归因和 postmortem 链接，禁止复述场景
- **复犯**：只追加频率标记（🟡×N / 🔴×N）
- **归属**：条目只写本范围内容，跨范围写到对应文件

### DEV-60 隔离对象但共享有状态引用 `🟢`

❌ 为每个连接创建独立 Pipeline 对象，但构造函数里 `self.x = ctx.x` 直接引用全局单例（Session/VAD），等于隔离了壳没隔离内容
✅ 创建隔离对象时区分有状态 vs 无状态组件。无状态（ASR/TTS/LLM）可共享，有状态（Session/VAD/StateMachine）必须每实例独立创建。检查清单：组件内部有 list/dict/counter/buffer → 必须独立
> 归因：只验证了"对象是否独立"，没追引用链验证"对象引用的组件是否也独立"

### DEV-66 实时通道断线缓冲未区分消息时效性 `🟢`

❌ WebSocket 断线时所有消息统一入队，实时音频帧无限堆积，重连后重放过期数据导致 ASR 错误和内存压力
✅ 断线缓冲必须区分消息类型：实时流数据（audio_chunk 等）断线即丢弃，只缓冲幂等控制消息；队列加上限防止无限增长
> 归因：通用缓冲逻辑未考虑消息时效性，把"不丢消息"当万能方案，忽略了实时流数据过期即无效

### DEV-71 流式状态机边界条件遗漏 `🟡×3`

❌ 只验证 happy path；输出侧和持久化侧各写一套清洗逻辑，遗漏不一致导致协议噪音进入对话历史
✅ 写/改流式逻辑后必须逐项验证：①操作顺序 ②标签跨 chunk ③EOF flush ④极端 chunk ⑤所有下游必须共用同一份 sanitize 函数，禁止各写各的
> 归因 B：DEV-71 第⑤条"用过滤后版本"不够精确，未强调共用同一函数。PR #24 CR R2 发现

### DEV-70 流式清洗逻辑未处理标签跨 chunk 拆分 `🟡×2`

❌ 清洗放在拆分后导致跨片段匹配失败；状态机只查找完整标签，标签被 chunk 边界拆开时永远无法闭合，后续文本全部丢弃
✅ 流式清洗必须在拆分前执行，且必须处理标签跨 chunk 拆分：用 pending 缓冲区保存不完整标签前缀，下次 chunk 拼接继续解析
> 归因 B：DEV-70 初版只覆盖了"跨片段"未覆盖"标签自身被拆开"。PR #24 CR 发现

### DEV-73 有状态组件放全局单例，跨连接共享脏状态 `🟢`

❌ ScreenSense（含帧缓冲区、触发计数器、冷却时间戳）放在全局单例中，重连或多客户端共享脏状态
✅ 有状态组件的生命周期必须匹配使用者：每连接独立的状态在 ws_handler 中创建，全局无状态引擎才放单例。检查清单：组件有 buffer/counter/timestamp → 不能放单例
> 归因：DEV-60 的变体，根因相同：没区分组件的状态生命周期。PR #23 CR 发现
