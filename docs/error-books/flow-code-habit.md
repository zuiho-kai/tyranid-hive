# 错题本 — 代码修改习惯

> 适用场景：改代码、改文档、修 P0/P1、批量操作

### 记录规则

- **条目结构**：❌ 错误做法 + ✅ 正确做法，整条控制在 **5 行以内**
- **❌/✅ 写法**：只写核心规则，禁止多层加粗嵌套（加粗最多一层）
- **`>` 注释**：只放归因和 postmortem 链接，禁止复述场景
- **复犯**：只追加频率标记（🟡×N / 🔴×N）
- **归属**：条目只写本范围内容，跨范围写到对应文件

### DEV-6 改代码不 grep 引用 + 不复用 pattern `🟡中频×3`

❌ 改签名不 grep 全量引用→下游断裂；不 grep 同类实现从零写；只改主调用链，遗漏次要入口
✅ 改前 grep 同类 pattern 复用→grep 全量引用→改后对照设计文档→涟漪推演。新增函数：先 Read 同文件已有函数对齐事务模式/错误格式/命名

### DEV-24 更新文档只改局部不扫全文 `🟡×4`

❌ 只改最明显的一处，其余过时内容全部遗漏；改了主索引（_index.md）但漏扫二级索引（flow-rules.md 等）；新增规则后未同步更新相关流程文档
✅ 更新文档前先全文扫描列出所有过时点，一次性全部更新。涉及索引体系时，主索引和所有二级索引必须同步扫描。**新规则落地时必须同步 grep 所有相关流程文档，确认无冲突**
> 归因 B：PR #26 CR 发现 flow-rules.md 条目归属未同步；PR #28 CR 发现 DEV-76 新规则未同步更新 worktree-workflow.md

### DEV-77 文档引用外部链接未验证有效性 `🟢`

❌ 写 README 时直接写 Releases/外部链接，未验证链接是否真实存在，导致死链上线
✅ 写文档前凡引用外部链接必须先验证：`gh release list`（Releases）/ `gh api repos/<owner>/<repo>`（仓库资源）/ WebFetch 实际访问确认
> 归因 A：禁止脑补规则有，但写 README 时未将"外部链接"纳入需要验证的范围。注意：禁止用 curl/wget 验证，走 gh CLI 或 WebFetch

### DEV-78 流程文档 shell 命令未标注执行目录 `🟢`

❌ 流程文档里的 shell 命令没有说明在哪个目录执行，读者按字面在 worktree 里执行 `git worktree remove` 报 Permission denied
✅ 流程文档每个 shell 代码块，凡涉及目录敏感操作必须在注释里标明执行位置（如 `# 在主仓库目录执行`）
> 归因 C：新场景，现有规则未覆盖"流程文档命令执行位置"

### DEV-80 实现前未横向对比同类项目方案 `🟢`

❌ 上来就读自己的代码、顺着现有实现优化，没有先搜同类项目怎么做，导致方向错误（如用 readPixels 做碰撞检测，而业界全部用几何 hitTest）
✅ 新需求开发或遇到"交互检测/渲染/协议"类问题，主动先搜 2~3 个同类开源项目的实现，确认方向正确后再动手。不要等用户提醒才去搜
> 归因 C：新场景，现有规则未覆盖"动手前横向对比"步骤

### DEV-29 P0/P1 修复列表漏项 + 执行碎片化 `🟢`

❌ CR 列了 5 个 P1 只修 4 个；独立修改逐个串行
✅ P0/P1 逐条核销不漏项；独立修改并行发出；独立文件更新并行

### DEV-47 批量/seed 幂等设计未考虑"部分成功" `🟢`

❌ 照搬 `count > 0` 跳过的幂等模式到有外部 API 依赖的场景，部分失败后永远无法补全
✅ 涉及外部依赖的批量操作用细粒度差集（按业务键比对），每条用 savepoint 包裹，单条失败不影响整体

### DEV-63 Review 评论不贴合现状时未做等价落地 `🟢`

❌ 只修字面上最容易对齐的 review 评论；评论和当前实现不完全贴合时，既不转译底层要求，也不明确驳回，结果漏掉真正要修的问题
✅ review 必须逐条核销。对不完全贴合现状的评论，先提炼底层要求，再选择等价修复或给出明确驳回理由，直到评论线程闭环

### DEV-69 文档中硬编码环境相关值（分支名、路径格式） `🟢`

❌ 文档示例中硬编码 `master`、Windows 反斜杠路径等环境相关值，换环境即失效
✅ 用占位符 `<主分支>` 加注释说明实际值，bash 代码块中路径统一用正斜杠
> 归因 C：新场景，无对应检查项。PR #17 CR 发现

### DEV-72 新功能前端无条件启动，未与后端配置协商 `🟢`

❌ 前端在连接建立后无条件启动截屏，后端 `screen.enabled=false` 时仍截屏发送，开关形同虚设
✅ 前端功能启动必须与后端配置协商：通过 health/capability API 获取开关和参数，后端说关就不启动
> 归因：只在后端做了开关，假设前端会"自然"配合，没考虑前端是独立进程需要显式协商。PR #23 CR 发现

### DEV-81 在错误链路上叠补丁不回退找根因（补丁螺旋）`🟢`

❌ 发现 A 不工作→补 B→B 引入新问题→补 C→循环。2 个分支 18 轮补丁，每次 commit 写"根治/彻底修复"但只修表面症状，底层平台限制从未被识别
✅ 连续 2 次补丁未解决时强制停下：①最小空白窗口验证平台行为 ②搜索平台已知 bug/限制 ③列出链路所有副作用 ④回退到最后正常状态换完全不同的链路
> 归因 C：Electron transparent 窗口 setPosition 触发假 resize 是 DWM 层限制，18 轮应用层补丁全部无效。正解是 Win32 WM_SYSCOMMAND SC_MOVE 一行调用。根本原因：未先验证平台限制就动手，每层补丁遮蔽上层问题

### DEV-82 平台 API 问题未做最小复现就动手修 `🟢`

❌ 在完整业务代码里反复调试平台行为（Electron/Win32/DWM），每次改动夹杂业务逻辑副作用，无法判断是平台问题还是自己的 bug
✅ 涉及平台 API 异常行为时，第一步建最小可独立复现的示例（脱离业务代码单独验证平台行为）。能复现 = 平台限制，不能复现 = 自己的 bug。确认后再回业务代码修
> 归因 C：拖拽 18 轮全在业务代码里调试，从未隔离验证 setPosition 本身是否触发 resize。最小复现 5 分钟就能定位

### DEV-83 同一功能/链路连续 3 次 fix commit 未强制停下 `🟢`

❌ 对同一功能/同一链路/同一文件簇连续提交 3+ 次 fix，每次 commit message 写"修复/根治"但问题依旧，没有客观信号触发回退
✅ 同一功能/同一链路/同一文件簇累计 3 次 fix commit 未解决 = 强制停下。执行：①`git log --oneline` 确认 fix 次数 ②列出每次 fix 改了什么、为什么没解决 ③回退到第一次 fix 前的状态 ④换完全不同的技术路径
> 归因 C：18 轮 commit 中多次写"根治""彻底修复"，但没有客观计数机制触发停下。需要硬性数字门槛

### DEV-84 平台能力边界未确认就写应用层代码 `🟢`

❌ 假设平台 API 按文档描述工作，直接写应用层逻辑。遇到异常后在应用层打补丁绕过，越绕越深
✅ 使用平台 API 前，先确认能力边界：①查官方文档已知限制 ②搜 GitHub Issues 同类问题 ③最小复现验证实际行为。三步都确认后再写应用层代码
> 归因 C：Electron transparent 窗口 + setPosition 的 DWM 限制在 GitHub Issues 里有大量报告，但从未搜索过。直接假设 setPosition 正常工作

### DEV-85 修复结论无证据，滥用"根治/彻底修复" `🟢`

❌ commit message 写"根治""彻底修复""完美解决"但没有验证证据，下一轮又回退。18 轮中至少 5 次写了"根治"
✅ 禁止在 commit message 中使用"根治/彻底修复/完美解决"等词，除非附带验证证据。commit message 只描述改了什么，不做效果承诺。效果验证写在 PR description 或测试日志里
> 归因 C：commit message 的乐观措辞制造虚假进展感，掩盖了"其实没解决"的事实，降低了回退决策的紧迫性

### DEV-86 重数据经渲染进程中转导致 UI 卡顿 `🟢`

❌ 截图 base64（MB 级）通过 IPC 传入渲染进程再 WebSocket 发出，阻塞渲染帧导致 Live2D 闪烁。把截屏从渲染进程移到主进程只解决了 IPC 阻塞，没解决截屏工具本身的问题
✅ 大体积数据（截图 base64、文件 buffer 等）禁止经过渲染进程中转。必须在主进程/后端完成采集和传输，渲染进程只发控制信号
> 归因 B：只关注了"数据在哪个进程流转"，没有追问"截屏工具本身是否有副作用"。正确做法是先验证截屏工具的行为再决定方案

### DEV-102 为了改动小而选择脏方案 `🟢`

❌ 选方案时优先考虑"改动最小"，结果选了在错误的层打补丁（如用 `screenshot-desktop` spawn cmd.exe 截屏，每次触发 DWM 合成刷新导致闪烁）。该换截屏实现却只移了调用位置
✅ 修 bug 时禁止为了"改动最小"而选择脏方案。该改架构就改架构，该换工具就换工具。改动大小不是目标，正确性才是
> 归因 A：DEV-81 的变体。知道 `screenshot-desktop` 底层是 spawn cmd.exe + C# exe，但为了"改动小"选择只移调用位置而不换实现。根因未解决，闪烁依旧

### DEV-94 README 完成状态与实际启用条件/实现方案不一致 `🟢`

❌ 功能标 ✅ 但未核实启用前提（需额外安装、配置开关）或文案描述与实际实现不符（写 readPixels 实际已换方案）
✅ 标 ✅ 前必须核实：①默认安装路径能否直接使用（检查 pyproject.toml extras / conf.example.yaml 默认值）②文案技术描述与当前代码一致（grep 实现确认）。有前提条件的在括号里注明
> 归因 C：PR #48 bot review 发现。浏览器操控需 extra + 配置启用但标了无条件 ✅；点击穿透写 pixi readPixels 但实际已改为前端命中检测

### DEV-88 未验证第三方库底层实现就采用 `🟢`

❌ 用 `screenshot-desktop` 截屏，没有先看它在 Windows 上的实现（spawn cmd.exe 执行 .bat 编译 C# 再运行），直接假设它是轻量级的纯内存操作
✅ 采用第三方库前，先查看其平台相关实现：①读源码确认底层机制 ②评估副作用（spawn 进程、文件 I/O、焦点切换等）③与需求匹配（高频定时调用 vs 一次性调用对性能要求不同）
> 归因 C：新场景。项目已有 koffi 加载 user32.dll/gdi32.dll 的先例，完全可以直接用 Win32 API（GetDC + BitBlt）做纯内存截屏，但没有先验证 screenshot-desktop 的实现就采用了

### DEV-103 副作用时序：先扣费/计数后调用外部服务 `🟢`

❌ 先执行副作用（扣费、计数、状态变更）再调用外部服务，失败时副作用已生效无法回滚
✅ 乐观预检 + 悲观执行：预检条件 → 调用外部服务 → 成功后才执行副作用
> 从 bot_civ DEV-37 搬运。CR checklist 增加"副作用时序检查"

### DEV-91 终端应用套用库的 optional deps 模式 `🟢`

❌ 把 pyautogui/pyperclip 等放进 `[project.optional-dependencies]`，但安装链路（bat/README）只跑 `uv sync`，导致功能可开启但运行时缺依赖。库的 optional deps 模式套到了终端应用上
✅ 终端用户应用的依赖全部放 `[project.dependencies]`，`uv sync` 一把装完。optional deps 只适用于公共库（让下游选择装什么）。决策前先问：这个项目的用户是开发者还是终端用户？
> 归因 A：过度设计。把"不是所有人都需要"的直觉套用了库的模式，没有考虑项目的安装链路和用户画像。PR #50 CR 发现

### DEV-92 默认值/fallback 覆盖用户显式配置 `🟢`

❌ `max(browser_rounds, desktop_rounds, 30)` 硬编码 30 作为兜底，用户显式配 `max_tool_rounds: 5` 被静默覆盖，配置形同虚设
✅ 写 max/min/fallback 时必须推演"用户显式配低值/高值"的场景。兜底值只在无有效配置时生效，不能与用户显式值取 max/min
> 归因 A：只考虑 happy path（"保底不能太低"），没推演用户故意限制的场景。PR #50 CR 发现

### DEV-93 配置变更 + 副作用操作失败时不回滚，前后端状态分裂 `🟢`

❌ `cfg.enabled = True` 后创建 provider 失败，config 保持 True 但实际 provider 为 None，前端显示"已启用"但功能不可用
✅ 配置变更 + 副作用操作必须原子化：副作用失败时回滚配置并返回错误。前端看到的状态必须与后端实际状态一致
> 归因 A：try/except 只 log warning 没回滚，没推演"前端看到什么 vs 后端实际是什么"。PR #50 CR 发现

### DEV-97 修 bug 缺边界分析，修复引入新问题 `🟢`

❌ P1 修复当"小修"，未对新代码做边界分析（null/作用域/类型），修完又冒出新 P1
✅ 每条修复必须列影响面 + 边界值分析。改一个变量的赋值语义 = 改变所有依赖它的代码行为
> 从 bot_civ DEV-BUG-19 搬运

### DEV-98 外部 client/资源未用 context manager + 返回值盲信 `🟢`

❌ new 外部 client 不关闭（资源泄漏）；信任外部 API 返回结构不做空值防御
✅ 凡 new 外部 client 默认 context manager；外部 API 返回值必做空值/空列表防御
> 从 bot_civ DEV-BUG-22 搬运

### DEV-99 大量 UI/前端改动一口气写完不分步检查 `🟢`

❌ 多个文件的 UI 改动一口气写完再验证 → 问题堆积
✅ 前端改动 >2 个文件时分步写分步检查，不一口气写完
> 从 bot_civ DEV-14 搬运

### DEV-100 多问题批量修复不分类并行 `🟢`

❌ 拿到多个问题逐个串行排查，不分类
✅ 先花 1 分钟分类（纯样式 / 前端逻辑 / 后端 / 配置），同类并行处理
> 从 bot_civ DEV-12 搬运

### DEV-101 API 边界防御缺失 `🟢`

❌ API 层只关注"能调通"，不加边界约束（非法值穿透、错误码映射不全）
✅ 写 API 端点时：①读 service 完整签名透传所有参数 ②数值字段加范围校验 ③错误语义映射正确状态码 ④错误映射写完后 grep 所有 raise/return error 逐条验证覆盖
> 从 bot_civ DEV-27 搬运

### DEV-107 E2E 测试未隔离被测 App 的自启动后端逻辑，导致 mock server 被杀死 `🟢`

❌ Electron App 内 `startBackend()` 无条件执行 `netstat` 查杀端口 12393 进程（mock server），再尝试启动 Python 后端（CI 无 runtime），WebSocket 连接失败。测试只验证了"mock server 能启动"，没考虑 App 自身会攻击该端口
✅ E2E 测试涉及"App 自带 server 启动逻辑"时，必须在 App 内加 `NODE_ENV=test` 守卫跳过真实后端启动。检查点：App 是否有端口清理/进程杀死逻辑？测试 mock 和 App 是否争用同一端口？
> 归因 C：新场景。测试隔离思路只覆盖了"mock server 不崩溃"，未推演"App 启动后会对 mock 做什么"。PR #68

### DEV-105 有参考源码时仍做多轮分析，不直接对照实施 `🟢`

❌ 用户已提供可直接参照的开源实现（如 clowder-ai），仍花费多轮时间做环境探测、最小复现、逐步验证，导致进度极慢
✅ 有参考源码时：①直接读关键函数（≤3个）②对照写代码 ③一次提交。分析环节只在参考源码缺失或存在平台差异时才执行
> 归因 A：DEV-80 变体。源码参考已经是"横向对比"的结果，不需要再重复分析流程

### DEV-104 代码搬迁只验功能可用，未检查隐式依赖在新上下文是否成立 `🟢`

❌ 把托盘菜单的 IPC handler 搬到独立设置窗口，只验证"功能能用"，没推演 `win` 的生命周期在新上下文是否仍然成立。主窗口销毁后设置窗口仍在，IPC 操作已销毁的 BrowserWindow 抛异常
✅ 代码从上下文 A 搬到上下文 B 时，必须逐项检查隐式依赖：①引用对象的生命周期是否仍一致 ②作用域/闭包捕获的变量是否仍有效 ③初始化时序是否仍保证 ④错误传播路径是否变化
> 归因 A：DEV-99 的变体。搬迁 = 大量改动，只验 happy path 没推演边界。PR #52 CR 发现
