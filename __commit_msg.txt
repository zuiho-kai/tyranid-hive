fix: 补全 NEXT_STEPS 五个缺口 — 进化闭环可自运转

N-2 (bug): OrganCrystallizer 全域查询修复
  - EpisodeStore.query_all() 新方法（不传 domain 过滤，真正查全部）
  - EpisodeStore.query_by_task() 新方法（任务连续失败检测用）
  - OrganCrystallizer 改用 query_all，不再传空字符串

N-1 (调度): _evolution_loop 定时任务
  - main.py 新增 _evolution_loop 协程（默认 10 分钟间隔，可 HIVE_EVOLUTION_INTERVAL 配置）
  - 每轮：scan_and_evolve + policy decay/retire + skill retire

N-3 (接入): WorldModel 进入执行流
  - dispatcher._build_enriched_message 末尾追加世界模型摘要
  - overmind 分析完成后自动更新 goal_tree 和 open_questions

N-4 (精度): Policy 命中逻辑升级
  - PolicyHitTracker 新增 trigger_conditions 评估器
  - _eval_conditions 支持 8 类结构化条件（多路径/线性依赖/失败率等）

N-5 (选人): Submind 按净值选人
  - mode_router._route_trial 先查 FitnessService 排行榜
  - 同域净值最高的两个 synapse 优先参赛，固定 fallback 保底
